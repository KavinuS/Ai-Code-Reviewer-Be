# Web framework and API layer
Django==6.1
djangorestframework==3.18.0
django-cors-headers==4.9.0

# Authentication
# Bearer tokens for the API; token_blacklist (a Django app it ships with) is
# what makes refresh-token rotation actually revoke the rotated-out token.
djangorestframework-simplejwt==5.5.1
# Server-to-server calls to the GitHub and Google OAuth endpoints. Only the
# backend ever talks to a provider, so no client secret reaches the browser.
requests==2.34.2

# Configuration
python-dotenv==1.2.3

# Database (PostgreSQL driver; used from Phase 5)
psycopg[binary]==3.3.4

# Cache (used from Phase 4)
redis==8.1.0
django-redis==7.0.0

# AI provider (used from Phase 2)
# Reviews run on Google Gemini. httpx is not listed separately: google-genai
# requires it and raises its timeout type, which ai_providers.py catches.
google-genai==2.21.0

# Deployment (Render)
# Django's runserver is a development server and says so; gunicorn is what
# actually serves the deployed process. The timeout has to clear the AI call:
# a review measured 40-82 seconds and gunicorn's default of 30 would kill the
# worker mid-request, which surfaces to the client as an empty response.
gunicorn==26.2.0
# A Render web service has no web server in front of it, so /static/ is served
# from inside the process. Without this the admin renders with no CSS.
whitenoise==6.12.0
